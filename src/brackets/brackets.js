'use strict';

/**
 * Check that following brackets are paired: (), [], {}.
 *
 * @param text
 *
 * @returns {Boolean} 'true' if the input text is valid and 'false' otherwise.
 */

function validate(text) {
  throw new Error();
}

module.exports = validate;

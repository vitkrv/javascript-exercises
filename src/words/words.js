'use strict';

/**
 * Group words on the basis of the usage of the same symbols with possible repeating
 *
 * @param {Array} sourceArray
 *            array of words
 *
 * @returns {Array} array of arrays with combined words
 *              for example, ['ab', 'bab', 'abc'] => [['ab', 'bab'], ['abc']]
 *
 * @throws Error if the array is empty
 */

function process(sourceArray) {
  throw new Error();
}

module.exports = process;

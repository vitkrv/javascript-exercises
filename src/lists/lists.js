'use strict';

/**
 * Merge two sorted lists using the specified comparator.
 *
 * @param {Array} left
 *            the left list.
 * @param {Array} right
 *            the right list.
 * @param {Function} comparator
 *            the comparator for elements.
 *
 * @returns {Array} the merged list.
 *
 * @throws Error if the any array is empty or comparator is not a function
 */

function merge(left, right, comparator) {
  throw new Error();
}

module.exports = merge;

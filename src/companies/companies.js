'use strict';

/**
 * Extract information about companies and departments from the array of users,
 * and return an object with companies and departments.
 *
 * User object looks like
 * {
 *  name: 'username',
 *  company: 'companyName' || null,
 *  department: 'departmentName' || null
 * }
 *
 * @param userArray
 *
 * @returns {*} Object where 'key' if company name and 'value' is array of departments
 */

function process(userArray) {
  throw new Error();
}

module.exports = process;
